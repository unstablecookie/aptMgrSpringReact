import React from 'react';
import './buttons.css';
import './button-blue.css'

const PlanButton = props => {
    return (
        <div className='example-wrapper-right-button'>
            <button className="modern-blue embossed-link-blue">plan</button>
        </div>
    );
};

export default PlanButton;