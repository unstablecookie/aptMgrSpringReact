import React from 'react';
import './buttons.css';

const ListPropertiesButton = props => {
    return (
        <div>
            <button class="modern embossed-link">list properties</button>
        </div>
    );
};

export default ListPropertiesButton;